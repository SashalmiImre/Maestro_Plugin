/**
 * Maestro Dashboard — ForgotPasswordRoute (placeholder)
 *
 * A `/forgot-password` route. Fázis 1 / B.4-ben kapja meg a tényleges
 * `account.createRecovery(email, DASHBOARD_URL/reset-password)` hívást.
 */

import React from 'react';

export default function ForgotPasswordRoute() {
    return (
        <div className="login-card">
            <div className="form-heading">Elfelejtett jelszó</div>
            <p style={{ color: 'var(--text-secondary, #999)' }}>
                Hamarosan elérhető (Fázis 1 / B.4).
            </p>
        </div>
    );
}
