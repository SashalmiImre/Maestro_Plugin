/**
 * Maestro Dashboard — RegisterRoute (placeholder)
 *
 * A `/register` route. Fázis 1 / B.4-ben kapja meg a tényleges form-ot
 * (név, e-mail, jelszó, AuthContext.register() hívás).
 */

import React from 'react';

export default function RegisterRoute() {
    return (
        <div className="login-card">
            <div className="form-heading">Regisztráció</div>
            <p style={{ color: 'var(--text-secondary, #999)' }}>
                Hamarosan elérhető (Fázis 1 / B.4).
            </p>
        </div>
    );
}
