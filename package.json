{
  "name": "maestro-workspace",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/Maestro/**"
    ]
  },
  "scripts": {
    "build:cf-validator": "node scripts/build-cf-validator.mjs",
    "check:cf-validator": "node scripts/build-cf-validator.mjs --check"
  }
}
