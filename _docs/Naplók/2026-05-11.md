---
tags: [napló, biztonság]
date: 2026-05-11
---

# 2026-05-11

Security audit S blokk **kick-off + S.0 + S.1 + S.2 + R.S.2.15 mitigated + S.2.5 cleanup CF**. Rebase utáni állapot: 6 commit `origin/main` előtt (a heti memo audit `5865b4a` auto-skip-elve a rebase-ben, mert a remote `16330db` ugyanazon Karbantartás.md változtatást tartalmazta).

## Mit csináltam

### (1) S.0 — Inventory + threat model (kick-off)

Új vault-jegyzetek:

- [[Komponensek/SecurityBaseline]] — STRIDE per komponens (proxy / dashboard / plugin / CF / Appwrite / Resend) + ASVS L2 + CIS IG1 mapping. Defense-in-depth.
- [[Komponensek/SecurityRiskRegister]] — 34 ismert gap (severity × likelihood × ASVS/CIS × owner × target × status).
- [[Szószedet]] új "Biztonság" szekció: STRIDE, ASVS L2, CIS IG1, CSP, HSTS, idempotency-key, PII-redaction, rate-limit, tenant-isolation, defense-in-depth.
- `_docs/Feladatok.md` új `S` szekció (S.0–S.14, 15 al-blokk).

Codex egyeztetés (`task-mp1jhbtd-u4mu2n`):
- 1. próba effort=xhigh teljes briefinggel → 3 perc futás után **context window kifutott** (sok `nl -ba` parancs)
- 2. próba effort=low ultra-rövid prompt + no file reads → **30s alatt válaszolt** részletes ASVS V-szakaszokkal + CIS Control # hivatkozva
- Codex módosítások: S.5 → CRITICAL feltételes, S.7 → HIGH (előrehozva), új S.12 (auth/session/access) + S.13 (logging/monitoring)
- Implementációs sorrend: **S.0 → S.1 → S.2 → S.7 → S.3 → S.4 → S.5 → S.12 → S.13 → S.6 → S.8 → S.9 → S.10 → S.11**

### (2) S.1 — Proxy hardening (CRITICAL)

[[Komponensek/ProxyHardening]] új jegyzet. `packages/maestro-proxy/server.js` (~310 sor változás):

- **S.1.1** origin whitelist (`https://maestro.emago.hu` + `http://localhost:5173` + UXP `null` origin), default-deny
- **S.1.2** `nullOriginGuard` middleware — UXP-only `X-Maestro-Client: indesign-plugin` header check ha origin null
- **S.1.3** rate-limit 7 path-szintű limiter (`express-rate-limit`, memory-store, dokumentált Redis upgrade Phase 2)
- **S.1.4** PII-scrub: `redactUrl()` URL-based + regex fallback (token / email / cookie / authorization / session / jwt / secret maszkolás)
- **S.1.5** **Deferred** — Codex pre-review nem indokolta változtatást, 30s uniform `/v1/*` timeout elegendő (trigger: első timeout-incident)
- **S.1.6** `injectAuthenticationFromQueryParams()` szűkítés: `/v1/realtime` only, 4KB cap, Appwrite session-key whitelist regex, raw-string fallback eltávolítva

Codex stop-time (`task-mp1k8f73-rdtj2x`): **1 BLOCKER + 4 MAJOR + 3 MINOR + 2 NIT mind javítva**. Verifying (`task-mp1kn806-x09za4`): 1 új MAJOR (`extractClientIp` XFF-first spoofolható) + 1 NIT (denyUpgrade flush) javítva (`proxy-addr` lib + `app.get('trust proxy fn')` + `socket.end()` graceful close). Egy MAJOR (`X-Maestro-Client` HMAC) **defer-elt Phase 2-re** — best-effort kliens-azonosító elfogadott pre-prod állapotban.

### (3) S.2 — CF rate-limit kiterjesztés (CRITICAL)

[[Komponensek/CFRateLimiting]] új jegyzet. `invite-to-organization/src/helpers/rateLimit.js` (~398 → ~267 sor refaktor), `actions/invites.js`, `actions/orgs.js`, `actions/schemas.js`:

- **S.2.1** `acceptInvite` IP rate-limit verify (5/15min/IP, 1h block) — a `checkRateLimit(ctx, 'accept_invite')` ténylegesen hívott
- **S.2.2** `invite_to_organization` multi-scope: `invite_send_ip` 30/15min + `invite_send_user` 50/24h + `invite_send_org_day` 200 email/24h
- **S.2.3** `delete_my_account` attempt-throttle: 3/5min/5min block (Codex stop-time MAJOR 3: NEM 24h hard cooldown — self-heal retry megengedhető)
- **S.2.6** Resend cost-control: folded into `invite_send_org_day` (200 email/24h, weight=validEmailCount). Codex M2: malformed email NE égesse a quotát → `EMAIL_REGEX` pre-filter rate-limit ELŐTT
- **S.2.7** Codex stop-time: 1 BLOCKER (block docId) + 3 MAJOR + 1 MINOR + 1 NIT — mind javítva. Harden adversarial: 2 MUST FIX (HIGH-2 storage fail-closed + HIGH-3 schema index) + 2 SHOULD FIX (MED-1 sendInviteEmail hook + LOW-1 wsUpgradeRateLimit hard cap) — mind javítva. Simplify (Reuse F2 + Eff F2 + F7): generic `evaluateAndConsume(ctx, scopes)` + `inviteSendScopes` factory + single-pass email-filter

**R.S.2.15 (paralel batch overshoot) Mitigated** — DESIGN-Q user-döntés option A:
- Best-effort: két paralel batch ~+5% overshoot a 200 email/24h limit fölött elfogadható
- Resend account-cap: külső infrastruktúra-szintű korlát (hard ceiling)
- Monitoring alert tervezve S.13-ban (anomaly detection)

Lásd [[Komponensek/CFRateLimiting#Accepted Risks]].

### (5) S.2.5 — Cleanup CF (lokálisan kész, még uncommitted)

Új scheduled CF [packages/maestro-server/functions/cleanup-rate-limits/src/main.js](../../packages/maestro-server/functions/cleanup-rate-limits/src/main.js):
- Cron `0 2 * * *` UTC napi, timeout 300s, `s-0.5vcpu-512mb`. Felvéve `appwrite.json`-be a többi cleanup CF után.
- `$updatedAt < cutoff` szűrés (jövőbiztos jelenlegi append-only counter-modellen, **kötelezően** korrekt a `setBlock` `document_already_exists` ág `updateDocument`-hosszabbítás miatt).
- Cap `MAX_DELETES_PER_COLLECTION=2_000` (Codex BLOCKER fix: 10_000 → 2_000 az 5min CF timeout miatt).
- `CLEANUP_DRY_RUN=1` env opcionális.
- Admin email triggers: `failedAny` (permission-misconfig) **VAGY** `cappedAtAny` (folytatandó) **VAGY** `totalDeleted >= 1_000` (anomáliás).

**Codex pipeline 3 iteráció**:
1. Pre-review (effort=low): 1 BLOCKER (`MAX_DELETES_PER_COLLECTION=10_000` cap → szekvenciálisan ~10 perc, NEM fér 5min timeout-ba) + 2 MAJOR (index hatékonyság leftmost-prefix szabály → Opció Y `$createdAt`/`$updatedAt` system-index proxy; admin email threshold-szemantika) + 1 MINOR (block-history audit-tradeoff S.10.5-ig) + 1 NIT
2. Implementáció + stop-time review: **1 BLOCKER** (`$createdAt` STALE block-doc-on a `setBlock` `document_already_exists` ág `updateDocument`-hosszabbítása miatt — `$updatedAt` az autoritatív) + 1 MAJOR (`failed > 0` NEM csendben success → `hasFailure` bővítés + admin alert trigger) + 1 MINOR tolerable + 1 NIT (`positiveIntEnv` helper a `parseInt(...) || default` 0/NaN edge-eset ellen)
3. Verifying review: **CLEAN** + 1 doku-drift NIT (admin-email triggerek doku 49-50. sor) — fixed

**R.S.2.5 closed**. S.2 audit-blokk logikailag zárva (S.2.4 login-throttle audit halasztva külön S.2.4-be).

### (4) Heti memo audit

`_docs/Karbantartás.md` frissítve: 15 memo átnézve, `dashboard-tenant-realtime` vault-érett (>2 hete, A.4.1 / A.4.2 lezárva — emelési kandidátus). Most a `5865b4a` és remote `16330db` ütközik a fájlon (manuális resolve PR előtt).

## Mi volt nehéz / blokkoló

1. **Codex effort=xhigh context-window timeout** (S.0): a teljes briefing + sok `nl -ba` parancs miatt 3 perc után kifutott. **Megelőzés**: effort=low + ultra-rövid prompt + no file reads minta — 30s válasz, részletes hivatkozásokkal. A következő security session-höz is bevált.
2. **R.S.2.15 paralel batch overshoot DESIGN-Q** — két konkurens CF-fütás között a counter increment-je race-elhet, +5%-ot átléphet. Resend account-cap az autoritatív, monitoring + alert (S.13) a felfedezés. User-döntés: best-effort, NEM kell distributed lock.
3. **Karbantartás.md merge konfliktus** — a remote `16330db` ("update maintenance memo date and add migration targets") egyidőben frissítette a fájlt, mint az én `5865b4a`-m. PR előtt manuális resolve szükséges.

## Új ismeret / felfedezés

- **Codex `effort=low` + ultra-rövid prompt + no file reads minta** (30s válasz részletes ASVS/CIS hivatkozással) — kanonikus a következő security audit sessionökhöz. Memo: [security-audit-s-blokk.md](security-audit-s-blokk.md).
- **`evaluateAndConsume(ctx, scopes)` generic multi-scope rate-limit API** + `inviteSendScopes` factory — több scope (IP / user / org-day) egy hívásban, atomi. Bevált minta minden új CF rate-limitre. Lásd [[Komponensek/CFRateLimiting]].
- **`proxy-addr` lib + `app.get('trust proxy fn')`** — XFF-spoofing védelem nélkülözhetetlen production proxy mögött. Lásd [[Komponensek/ProxyHardening#Codex verifying review]].
- **`nullOriginGuard` minta** UXP kliensre — origin null + secondary header check egy jobb default-deny pattern, mint a `cors` lib `null` accept-je.

## Holnap (vagy következő session)

A `## Holnap` szakasz nyitott — három lehetséges next-step, **user-döntés**:

1. **S.7 — Cross-tenant adversarial (HIGH, 2 session)** — a Codex előrehozta. 6 al-pont: rowSecurity audit, per-tenant ACL backfill verify, Realtime channel filter audit, cross-org membership ACL, **2-tab adversarial teszt** (`localStorage.maestro.activeEditorialOfficeId` csere), stop-time review. Új jegyzet: [[Komponensek/TenantIsolation]].
2. **Karbantartás.md merge + PR open** — a `_docs/Karbantartás.md` ütközés manuális resolve, majd PR `origin/main` felé (5 commit). PR előtt **destruktív flow**: user-trigger szükséges.
3. **CF deploy (S.2.2/2.3/2.6 + cleanup)** — a 4 érintett CF (`invite-to-organization`, `user-cascade-delete`, esetleg cleanup-rate-limits) éles deploy. **Élesedés előtt kötelező S.2.5** (cleanup CF) — különben 30 nap után counter-doc-ok napi ~500/CF tempóval halmozódnak.

A Codex prioritás szerint **S.7 elsőre**.

## Kapcsolódó

- **Commit-ok** (post-rebase hash-ek, 6 commit `origin/main` előtt):
  - `213e324` feat(security): S blokk audit roadmap + S.1 proxy hardening (Codex CLEAN)
  - `f770768` feat(security): S.2 CF rate-limit (S.2.2/2.3/2.6 + S.2.7 Codex CLEAN)
  - `a8c7946` harden(security): S.1+S.2 adversarial + simplify (4 fix + CLEAN)
  - `9b66ba3` docs(security): R.S.2.15 decision mitigated — DESIGN-Q user option A
  - `2848310` feat(security): S.2.5 cleanup-rate-limits scheduled CF (Codex CLEAN)
  - `9264743` docs(napló): 2026-05-11 daily note (jelen jegyzet)
- **Érintett komponensek**: [[Komponensek/SecurityBaseline]], [[Komponensek/SecurityRiskRegister]], [[Komponensek/ProxyHardening]], [[Komponensek/CFRateLimiting]]
- **Friss ADR-ek**: — nincs új ADR (S blokk Komponensek/-szintű hosting, ADR-ek per al-blokk később, ha architektúra-szintű döntés keletkezik)
- **Memo**: [security-audit-s-blokk.md](security-audit-s-blokk.md)
