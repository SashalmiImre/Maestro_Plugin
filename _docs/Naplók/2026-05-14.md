---
tags: [napló, biztonság, frontend, acl]
date: 2026-05-14
---

# 2026-05-14

**S.7.7 frontend tenant `createRow` / `createDocument` ACL fix (R.S.7.3 close kódoldal)**. 6 fájl + 2 új helper, 7 hívóhely. Codex stop-time NO-GO (2 BLOCKER → deploy-prerequisite al-pontok S.7.7b + S.7.7c + 1 MAJOR javítva + 1 MINOR S.7.5 lefedi).

## Mit csináltam

### (1) Inventory + Codex pre-review

3-explorer agent felmérte a frontend `createDocument` / `createRow` hívásokat. 7 hívóhely (4 plugin + 3 dashboard, mind office-scope user-data collection). Pre-review (Codex effort=low) 5 design-Q-ra GO:
- **Q1**: helper-distribution **A** (két különálló `teamHelpers.client.js`, mint server-side mirror) — bizotos szigeteltség
- **Q2**: `documentSecurity:true` verify külön S.7.7b al-pont (NE blokkolja a kód-fixet)
- **Q3**: validation hookok internal `useScope()`+`useUser()` — kanonikus minta
- **Q4**: `withCreator` MANDATE frontend-en is (ADR 0014 kanonikus, defense-in-depth)
- **Q5**: NO new test infra (manual review + S.7.5 2-tab adversarial elég)

Findings: 1 BLOCKER (fail-closed) + 3 MAJOR (single snapshot, hooks top-level, office-not-org scope) + 2 MINOR + 1 NIT — mind beépítve az implementációba.

### (2) Implementáció (6 fájl, 7 hívóhely)

| # | Fájl | Változás |
|---|---|---|
| 1 | `packages/maestro-indesign/src/core/utils/teamHelpers.client.js` | **Új** (69 sor, 3152 B) — `buildOrgAclPerms`, `buildOfficeAclPerms`, `withCreator`, fail-closed validation |
| 2 | `packages/maestro-dashboard/src/utils/teamHelpers.client.js` | **Új** (69 sor, 3131 B) — identikus API |
| 3 | `packages/maestro-indesign/src/core/contexts/DataContext.jsx` | `withScope` → `buildTenantDocOptions`, `useUser` import + userRef, `createArticle` + `createValidation` |
| 4 | `packages/maestro-indesign/src/data/hooks/useOverlapValidation.js` | `useScope`+`useUser` import, hookon belül ref-ek + perm-snapshot a `persistToDatabase` elején, `persistStructureValidation` 3rd param `permissions` |
| 5 | `packages/maestro-indesign/src/data/hooks/useWorkflowValidation.js` | Mint #4 (preflight flow) |
| 6 | `packages/maestro-dashboard/src/contexts/DataContext.jsx` | `useAuth` import + userRef, `withScope` → `buildTenantDocOptions`, `createPublication`/`createLayout`/`createDeadline` |

**Belső helper `buildTenantDocOptions(data)`** (mindkét DataContext): egyetlen snapshot a callback elején, `{data, permissions}` return → fail-closed throw bármelyik scope/user hiányára. Codex MAJOR (single snapshot vs interleaving race) absorb.

**Validation hookok fail-strategy**: `persistToDatabase` callback elején snapshot+perms-build, hiányzó scope/user esetén `logError(...) + return Promise.resolve()` (silent log+skip, NEM throw) — fire-and-forget background, throw a `pageRangesChanged` event handlerből UX-kárt okozna.

### (3) Tooling-megfigyelés: Edit/Write tool cache-illuzórikus session-bug

Session során az `Edit` és `Write` toolok "successfully" üzenettel jelezték a fájl-módosítást, de a Bash `cat`/`sed`/`grep` (fájlrendszerről olvas) azt mutatta, hogy a változások NEM mentődtek le. A `Read` tool a saját buffer-cache-ből játszott vissza HAMIS frissített tartalmat. **Megoldás**: minden 6 fájl-módosítás Bash heredoc-kal + Python script-tel a fájlrendszerre — közvetlen verify utána. Tooling-bug, NEM kódolási hiba.

### (4) Codex stop-time review — NO-GO

Codex `a9b41e77813b3cbce` (effort=low, no file reads):
- **BLOCKER 1**: `documentSecurity:true` flag NEM verifikált a 6 collection-en (ADR 0014 Layer 1 prerequisite). Ha bármelyik flag `false`, a S.7.7 fix doc-szintű ACL ignorálódik. → Új R.S.7.6, új al-pont S.7.7b (Feladatok.md), DEPLOY-BLOCKER.
- **BLOCKER 2**: Legacy doc-ok üres `permissions`-szal a 6 collection-en — S.7.2 backfill NEM fedte le. → Új R.S.7.7, új al-pont S.7.7c, DEPLOY-BLOCKER.
- **MAJOR**: Dashboard `useAuth() ?? {}` masks provider misconfiguration (ACL határon). → Javítva: `const authValue = useAuth(); if (!authValue) throw new Error('DataProvider must be mounted inside AuthProvider (S.7.7 ACL invariant).');`.
- **MINOR**: Realtime smoke teszt gap. → S.7.5 al-pont kibővítve a 6 user-data collection-re (Realtime push + REST `listDocuments`).

Code-fix kész. **Production close pre-requisite**: S.7.7b + S.7.7c + S.7.5 mind kész.

### (4b) Codex verifying review — C1+C3+C4 GO, C2+C5 NEEDS-WORK

Codex `aae5da393f1dd5c0c` (effort=low, no file reads):
- **C1 GO**: MAJOR fix korrekt (`if (!authValue) throw`). React 18 StrictMode dupla-hívás OK.
- **C2 NEEDS-WORK**: plugin `useUser()` symmetry guard hiányzik. → Javítva: `const userValue = useUser(); if (!userValue) throw new Error('DataProvider must be mounted inside UserProvider (S.7.7 ACL invariant).');`. Intentional invariant a destructure-on-undefined TypeError helyett (ASVS V14.2.1).
- **C3 GO**: deploy-blocker narrative (Feladatok + RiskRegister) korrekt.
- **C4 GO with caveat**: `[x] Done (code-only)` + "Deploy halasztva — pre-requisite" suffix elfogadható (ASVS V1.1.4 traceability).
- **C5 NEEDS-WORK**: S.7.7c spec túl-vague `doc.createdBy ?? owner`-on. → Bővítve: explicit 2-fokozatú fallback (1. `createdBy` valid → `withCreator`; 2. invalid/missing → CSAK office team-perm, NINCS user-read inference); minden fallback-doc per-collection+per-$id `fallbackUsedDocs[]` audit-array (ASVS V4.1.3 + V1.4.4).

### (4c) Shared helper refactor (user-trigger, 2026-05-14)

User-kérdésre kiderült: a két helper-fájl (plugin + dashboard) diff-je **1 sor** (csak a header `@description` szóhasználata). Refactor: `maestro-shared`-be áthelyezés.

- Új: `packages/maestro-shared/teamHelpers.client.js` (74 sor, identikus tartalom, egyesített header)
- `maestro-shared/package.json` bővítve: új `exports`-bejegyzések (`./teamHelpers.client` + `./teamHelpers.client.js`) + új `peerDependencies: { "appwrite": "^24.1.1" }` (mind a plugin, mind a dashboard amúgy is `^24.1.1`-et bundlel, így nem dupla-bundle)
- 4 import-csere: plugin `DataContext.jsx` + 2 hook + dashboard `DataContext.jsx` mind `import { ... } from 'maestro-shared/teamHelpers.client.js'`-re vált
- 2 régi helper-fájl törölve: `packages/maestro-indesign/src/core/utils/teamHelpers.client.js` + `packages/maestro-dashboard/src/utils/teamHelpers.client.js`

**Indok**: a Codex pre-review GO A-t adott (külön fájlok), DE a stop-time után látszott, hogy a két fájl 99% identikus → DRY-win + drift-kockázat megszűnik + ADR 0014 invariáns 1 helyen ellenőrizhető + jövőbeli S.7.7c / új tenant-érintő frontend `createX` automatikusan ugyanazt használja. Codex GO "B is reasonable later" megfogalmazás megengedte.

### (4d) Harden pipeline (7-fázis, Codex CLEAN)

A teljes uncommitted halmazon végigfutott egy harden pass (skill `/harden`, 7 fázis):

| Fázis | Eszköz | Eredmény |
|---|---|---|
| 1. Baseline | Codex `a01554cc` (effort=low) | CLEAN: 0 BLOCKER, 0 MAJOR, 2 MINOR (`.client` boundary + log-severity), 1 NIT (helper whitespace asymmetry) |
| 2. Adversarial | Codex `af8b038b` | 1 MUST FIX (A4 documentSecurity:true continuous verify — már S.7.7b), 3 SHOULD CONSIDER (A2/A3/A5 design-Q-k) + 4 új finding |
| 3. Szintézis + AskUserQuestion | — | User-decision A: NIT fix + S.7.7d új al-pont; spawn task: dashboard `console.*` cleanup **(elindult, majd policy-check után dismissed — lásd alább)** |
| 4. Phase 4 fix | — | Shared helper whitespace-strict symmetry (`buildOrgTeamId` / `buildOfficeTeamId` + `withCreator` 3-way symmetric); S.7.7d Feladatok-ba; `spawn_task` chip |
| 5. Simplify pass | 3 párhuzamos agent (Reuse / Quality / Efficiency) | F1+#3 HIGH: extract `useTenantAclSnapshot('tag')` shared hook → 2 validation hook ~-30/-25 sor + ACL invariáns 1 enforcement-point; #6 HIGH: comment trim Codex/date tag-ek (5 helyen) |
| 6. Verifying | Codex `a7f2bc2e` | **GO CLEAN** mind az 5 Q-ra, 0 új finding |
| 7. Összefoglalás | — | Harden lezárva, commit halasztott (user-trigger) |

**Új fájl**: `packages/maestro-indesign/src/data/hooks/useTenantAclSnapshot.js` (50 sor) — közös perms-build hook a 2 validation hook számára. Tag-paraméter logger-prefix-hez. Fail-closed `null` return + `logError` (NEM throw — fire-and-forget background flow).

**Refactor net**: `useOverlapValidation` -27/+5 sor, `useWorkflowValidation` -22/+5 sor, plus 50 sor új hook = ~+1 sor összesen, de **ADR 0014 invariáns 1 enforcement-point** (a shared hook) helyett 2 inline minta.

### (4e) Dashboard `console.*` cleanup — STOP (policy-elfogadott)

A Harden pipeline Phase 3-ban a baseline review out-of-scope-ként jelezte: a `packages/maestro-dashboard/src/contexts/DataContext.jsx`-ben 6 `console.error`/`console.warn` hívás (line 409/491/528/560/887/957). Spawn task elindítva (külön branch `chore/dashboard-logger-cleanup` a `main`-ből, új worktree).

**STOP** — preflight grep felfedte:

- `packages/maestro-dashboard/CLAUDE.md` line 39 **explicit megengedi**: *"A dashboardon `console.*` elfogadott (nincs UXP logger)"*. Ez egy dokumentált policy decision (a böngészőben a DevTools console natívan elérhető, nincs UXP-environment-specifikus logger-igény, mint a plugin-en).
- Teljes `console.*` count a dashboard `src/`-ben: **62 db** — a 6 csak töredék.
- Nincs `logger.js` / `import.*log.*from` minta a dashboard `src/`-ben.

**User-decision A (2026-05-14)**: hagyjuk — dashboard policy `console.*`-ot támogatja. A `chore/dashboard-logger-cleanup` branch + worktree törölve (`git worktree remove` + `git branch -D`). NEM volt módosítás.

**A gyökér `CLAUDE.md` "Soha `console.*`" szabálya érvényes a pluginra**, mert ott UXP environment-specifikus loggert használunk (`log()`/`logError()` a `core/utils/logger.js`-ben). A dashboard-on a böngésző-natív `console.*` az elfogadott.

### (5) Doku-frissítések

- `_docs/Feladatok.md`: **S.7.7 `[x]` Done (code-only)** + új S.7.7b + S.7.7c (DEPLOY-BLOCKER) + S.7.5 kiterjesztés (6 collection)
- `_docs/Komponensek/TenantIsolation.md`: új "S.7.7 frontend ACL fix" szekció (7-hívóhely tábla + Codex pipeline + deploy-blokkolók)
- `_docs/Komponensek/SecurityRiskRegister.md`: R.S.7.3 **Closed (code-only)** + új R.S.7.6 + R.S.7.7 (DEPLOY-BLOCKER)
- `_docs/Naplók/2026-05-14.md` (ez a fájl)

## Holnap (vagy következő session)

**Auto-mode prioritás**:

1. **Commit + push** S.7.7 + Codex stop-time fix + doku (user-trigger, NEM commit-oltam automatikusan)
2. **S.7.7b** — `documentSecurity:true` verify a 6 collection-en (manuális Console VAGY új CF helper)
3. **S.7.7c** — `backfill_acl_phase3` action design + Codex pre-review (vagy phase2 bővítés)
4. **S.7.5** — Adversarial 2-tab teszt a 6 collection-en (user-task fejlesztői env-en)
5. **S.7.8** — Phantom-org window (R.S.7.4, cross-cutting)
6. **S.7.9** — `anonymize_user_acl` (R.S.7.5, Q1 user-decision B)

## Kapcsolódó

- **Commit-history** (`claude/zealous-euler-00c483`, NEM commit-olva — 15 új fájl-szintű módosítás)
- **Codex agent ID-k**: pre-review `aeddc5f425237dc40`, stop-time `a9b41e77813b3cbce`
- **Érintett komponensek**: [[Komponensek/TenantIsolation]] (S.7.7 szekció), [[Komponensek/SecurityRiskRegister]] (R.S.7.3 closed, R.S.7.6 + R.S.7.7 open)
- **ADR**: [[Döntések/0014-tenant-doc-acl-with-creator]] (Layer 1+2+3 invariáns)
- **Memo**: [security-audit-s-blokk.md](security-audit-s-blokk.md)
